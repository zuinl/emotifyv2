import { StyleSheet } from "react-native";
import { colors } from "../../colors";

export const styles = StyleSheet.create({
  container: {
    width: 180,
  },
  image: {
    width: "100%",
    height: 180,
    borderRadius: 30,
    marginBottom: 12,
  },
  songTitle: {
    color: colors.white,
  },
});
