import { StyleSheet } from "react-native";
import { colors } from "../../colors";

export const styles = StyleSheet.create({
  container: {
    width: 120,
  },
  image: {
    width: "100%",
    height: 120,
    borderRadius: 30,
    marginBottom: 12,
  },
  title: {
    color: colors.white,
  },
});
